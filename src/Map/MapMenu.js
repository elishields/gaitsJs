// Import classes from React
import React, { Component } from "react";

// Import components and objects
// import {APIarray} from "../APIarray";

// Import styling
import "../App.css";

/*
    * MapMenu.
    * Renders <MapMenu />
    * Called by <Map />
*/
export class MapMenu extends Component {

    // Stateless and functionless constructor for <MapMenu />
    constructor(props) {
        super(props);
        this.state = {
        };
    };

    // Renders
    render() {
        return (
            <div>

            </div>
        )
    };
}

export default MapMenu;